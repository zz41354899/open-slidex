# 效能重構與驗證紀錄

日期：2026-09-08。範圍：本輪盤點的 10 項瓶頸，以及 Unsplash 重複搜尋。本機原有簡報與無關工作區變更均保留；未發布或部署。

## 已完成修正

| 項目 | 實作 |
| --- | --- |
| 選取狀態 API 過密 | React effect 僅依賴選取識別值；請求防抖、序列化並保留最後一次；伺服器略過相同內容的 current.json 寫入。 |
| SSE 重複讀取文件 | 檔案事件與客戶端讀取合併；限制單一在途讀取；檢查修訂與儲存狀態，避免舊回應覆蓋新資料。 |
| 重複解析／驗證 | TSX 轉換、逐頁解析與完整驗證摘要使用有容量上限的快取；可變回傳物件隔離，避免污染快取。 |
| 編輯時計算阻塞 | 逐頁增量正規化並重用未修改頁；延遲建立驗證 Worker；儲存時重用驗證結果並維持修訂衝突檢查。 |
| Workspace 清單與封面反覆解析 | 以檔案指紋快取摘要；清單 I/O 限制並行數；封面先檢查指紋再開啟文件。 |
| HTML 縮圖重複工作 | 快取 HTML 分析、合併相同來源的渲染工作；同批多頁共用瀏覽器頁面；納入本機附屬資源指紋，支援 ETag／304。 |
| 媒體整檔讀入記憶體 | 改用檔案串流；支援 Range、HEAD、If-Range、ETag／304 與無效區間 416。HTML 沙箱路徑保留。 |
| 圖表屬性面板提前載入 | 真正使用 lazy／Suspense；打包版證實未選取前沒有請求圖表面板 chunk。 |
| Undo 保存完整文件 | 改為反向差異與最新檢查點，限制 80 筆及估算 8 MiB；保持逐字元精確復原。 |
| 閒置 editor router／watcher | 閒置 5 分鐘且無在途請求／SSE 連線才回收；每 30 秒掃描，關閉時清理計時器與監聽。 |
| Unsplash 重複搜尋 | 60 秒快取、同時請求去重與容量上限；依憑證及查詢隔離，失敗可重試；保留下載追蹤與來源紀錄。 |

打包回歸另發現模組循環初始化問題：調整強制分包邊界，匯出用 shader 字串延至首次匯出才建立。

## 驗證結果

- `npm run build:runtime` 通過。
- `npx tsc --noEmit` 與 `git diff --check` 通過。
- 完整 `npm run test:source`：377 項通過，0 失敗（MCP 24、建置腳本 13、SDK／Workbench／CLI 340）。
- 新增測試涵蓋請求合併、快取失效與物件隔離、差異式歷史紀錄、檔案串流／條件請求、閒置回收、HTML 多頁縮圖及附屬圖片更新、Unsplash 搜尋去重。
- 本機 Chrome 使用已打包的前端及隔離暫存簡報測試 30／120 頁：從第 1 頁切到最後再返回；Code editor 編輯、自動儲存、復原、重做、圖表延遲載入及 Worker 驗證均通過；前端錯誤為 0。
- 兩種頁數皆測得：連續 20 次外部檔案更新合併成 1 次文件 GET，畫面顯示第 20 次的最後內容。

### 微基準

Node 本機暖快取測試，中位數；不是瀏覽器 FPS，也不是首次冷啟動數據：

| 頁數 | 重複解析 | 重複驗證摘要 | 單頁變更後增量正規化 |
| --- | ---: | ---: | ---: |
| 30 | 0.93 ms | 0.74 ms | 1.46 ms |
| 120 | 3.59 ms | 2.80 ms | 3.09 ms |
| 300 | 8.97 ms | 6.94 ms | 5.09 ms |

本輪先前量到的 300 頁完整驗證摘要約 77.33 ms；重複同源驗證現在約 6.94 ms。未變更的 30 頁文件重開約 0.08 ms，先前約 16.81 ms。這些改善來自避免重複工作，不能推論所有冷路徑或互動都同比例加速。

## 重跑方式

```sh
npm run build:runtime
npx tsc --noEmit
OPEN_SLIDEX_CHROMIUM_EXECUTABLE='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' npm run test:source
node --import tsx scripts/verify-workbench-performance.mts
git diff --check
```

瀏覽器腳本建立並清除專用暫存簡報，不編輯使用者的 Workspace 簡報。Unsplash 去重由注入的模擬 fetch 驗證，未呼叫付費或憑證式外部服務。尚未宣稱正式發布版、長時間壓力測試或所有硬體上的效能結果。
