interface:
  display_name: "OpenSlideX Source Import"
  short_description: "Convert local PowerPoint into editable OpenSlideX"
  default_prompt: "Use $slidex-source-import to convert the supplied local PPTX into an editable native OpenSlideX deck while preserving recovered text and image frames."
